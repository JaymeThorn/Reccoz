import 'package:flutter/material.dart';

class EditProfileTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Edit Profile Tab'),
      ),
    );
  }
}
